export default function Configuration() {
    return (
        <main style={{ padding: "32px" }}>
            <h1>Configuration</h1>
            <p>Configuración de fuentes, cantidad de posts y estado del sistema.</p>
        </main>
    );
}